ArrayList <Object> o = new ArrayList <>();
o.add ("test"); // String
o.add (15); // Integer
o.add ('c'); // Chatracter

for (int i = 0; i < i.size(), i++)
{
   System.out.println(o.get(i)); // Runtime polymorphism
}